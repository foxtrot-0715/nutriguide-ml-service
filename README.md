# 🥗 NutriGuide ML Service

NutriGuide — это интеллектуальная микросервисная платформа для глубокого анализа рациона питания. Сервис позволяет пользователям получать прогнозы нутриентов на основе нейросетевых моделей, управляя своим прогрессом через личный кабинет.

## 🚀 Ключевые возможности
* **Интеллектуальный анализ**: Обработка текстовых данных о приеме пищи с помощью асинхронного ML-движка.
* **Асинхронная архитектура**: Использование RabbitMQ гарантирует стабильность API даже при высоких нагрузках на нейросеть.
* **Экономическая система**: Встроенный биллинг с балансом кредитов, списанием за операции и возможностью пополнения счета.
* **Личный кабинет**: Веб-интерфейс для регистрации, мониторинга баланса и просмотра детальной истории операций в реальном времени.

## 🛠 Технологический стек
* **Backend**: FastAPI (Python 3.10) + SQLAlchemy 2.0
* **Frontend**: Jinja2 + Bootstrap 5 + Vanilla JS
* **Database**: PostgreSQL
* **Infrastructure**: RabbitMQ (брокер сообщений), Docker & Docker Compose
* **Security**: Хэширование паролей (Passlib) и валидация данных (Pydantic)

## 📦 Быстрый старт
1.  **Клонирование и запуск**:
    ```bash
    docker compose up --build
    ```
2.  **Доступ к интерфейсу**:
    Откройте браузер по адресу: `http://localhost:8000/`
3.  **API Documentation**:
    Интерактивная документация Swagger доступна по адресу: `http://localhost:8000/docs`

## 🏗 Архитектура проекта
Проект организован по принципу разделения ответственности:
- `/src/database`: Модели данных и логика работы с PostgreSQL.
- `/src/schemas`: Pydantic-схемы для строгой валидации API.
- `/src/templates` & `/src/static`: Компоненты веб-интерфейса.
- `worker.py`: Автономный воркер для фоновой обработки ML-задач.
