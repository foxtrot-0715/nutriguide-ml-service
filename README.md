Проект: NutriGuide ML Service 🍏🤖

NutriGuide — это микросервисное приложение для анализа нутриентов (БЖУ и калорий) в продуктах питания на основе произвольного текстового ввода пользователя.
🛠 Технологический стек:

    Backend: FastAPI (Python 3.10+)

    Message Broker: RabbitMQ (для асинхронной обработки задач)

    Database: PostgreSQL (хранение задач и баланса пользователей)

    ML Engine: Scikit-learn (Ridge Regression + TF-IDF Vectorization)

    Containerization: Docker & Docker Compose

🧠 Особенности ML-реализации:

В проекте реализована полноценная модель машинного обучения, способная предсказывать пищевую ценность даже для тех блюд, которых нет в базе в явном виде.

    Модель: Ridge Regression (Линейная регрессия с L2-регуляризацией), выбранная за стабильность и высокую скорость работы.

    Обработка текста: TF-IDF Vectorizer для преобразования названий блюд в числовые векторы.

    Датасет: Модель обучена на выборке из ~7000 записей о продуктах и блюдах.

    Оптимизация: Модель сохранена в формате .joblib и автоматически подгружается воркером при старте.

🏗 Архитектура:

    FastAPI принимает запрос, проверяет баланс пользователя и отправляет задачу в очередь RabbitMQ.

    Worker (этот сервис) слушает очередь, при получении задачи выполняет предсказание через ML-модель и сохраняет результат в PostgreSQL.

    Реализована система Refund: если ввод некорректен или произошла ошибка, списанные кредиты автоматически возвращаются на баланс пользователя.

🚀 Как запустить:

    Клонируйте репозиторий.

    Убедитесь, что файл модели находится в app/models/food_model.joblib.

    Запустите всю систему одной командой: docker compose up --build

В ходе разработки была решена проблема несовместимости последних версий библиотек машинного обучения с набором инструкций старых процессоров (ошибка Illegal Instruction). Решение было найдено путем точного подбора версий scikit-learn и numpy, что позволило запустить ML-анализ в изолированном Docker-контейнере без потери точности предсказаний
